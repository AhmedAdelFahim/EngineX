#!/usr/bin/env bash

#        fields=($input)
#        number_fields=${#vars[@]};
#        echo ${fields[${number_fields}-1]}

#        if ! [[ ${fields[${number_fields}-1]} =~ \; ]]
#        then
#            echo "wrong query please enter correct query";
#            continue;
#        elif [[ ${number_fields} > 0 && ${fields[${0}]} =~ CREATE ]];
#        then
#            if [[ ${number_fields} > 1 && ${fields[${1}]} =~ TABLE ]]
#            then
#
#            fi
#        fi
#        echo "Number of words in var: '${#vars[@]}'"
#        if []; then
#
#        fi
#        echo $input | awk  'BEGIN {
#            FS=" ";
#        } {
#            if($1 ~/CREATE/) {
#                pp;
#            }
#        } END {
#
#        }';